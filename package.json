{
    "scripts": {
    "frontend": "cd front-cli && npm start",
    "api": "cd back && nodemon index.js",
    "dev": "concurrently --kill-others-on-fail \"npm run api\" \"npm run frontend\"",
    "heroku-postbuild": "cd front-cli && npm install && npm run build"
  }

}